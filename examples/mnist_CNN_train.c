#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

#include "cnn.h"
#include "ndarray.h"
#include "misc.h"

#define image_size 20
#define MAX(x, y) (((x) > (y)) ? (x) : (y))
